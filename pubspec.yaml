name: music_player
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  audio_video_progress_bar: ^2.0.1
  cached_network_image: ^3.3.1
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  just_audio: ^0.9.36
  just_audio_background: ^0.0.1-beta.11
  percent_indicator: ^4.2.3
  provider: ^6.1.1
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # to include all images of assets
  
